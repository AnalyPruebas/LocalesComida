export enum ESPACIO_LUGAR {
  abierto = 0,
  cerrado = 1,
}

export enum TIPO_LUGAR {
  restaurante = 0,
  comidaRapida = 1,
}

export enum ESTADO_RESERVA {
  pendiente = 0,
  realizado = 1,
  cancelado = 2,
}
